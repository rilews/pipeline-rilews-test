services:
  calculator:
    image: ghcr.io/rilews/pipeline-rilews-test:${IMAGE_TAG:-latest}
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - ./calculadora/.env
    restart: unless-stopped
